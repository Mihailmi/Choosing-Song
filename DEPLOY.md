# 🚀 Развертывание на Render

Инструкция по развертыванию веб-приложения для поиска песен на Render.com.

## 📋 Предварительные требования

1. **Аккаунт на Render.com** - зарегистрируйтесь на [render.com](https://render.com)
2. **GitHub репозиторий** - ваш код должен быть в GitHub
3. **Google API ключ** - для работы с Gemini API
4. **Подготовленные файлы индекса** - `Data/songs_index.faiss` и `Data/songs_metadata.json`

## ⚠️ Важно: Файлы индекса

Перед развертыванием убедитесь, что файлы индекса созданы и закоммичены в репозиторий:

```bash
# Локально создайте индекс (если еще не создан)
cd Source
python prepare_embeddings.py

# Проверьте, что файлы существуют
ls ../Data/songs_index.faiss
ls ../Data/songs_metadata.json
```

**Важно:** По умолчанию эти файлы в `.gitignore`. Для развертывания на Render их нужно закоммитить:

1. Временно уберите из `.gitignore`:
   ```bash
   # Закомментируйте эти строки в .gitignore:
   # Data/songs_index.faiss
   # Data/songs_metadata.json
   ```

2. Добавьте файлы в репозиторий:
   ```bash
   git add Data/songs_index.faiss Data/songs_metadata.json
   git commit -m "Add index files for deployment"
   git push
   ```

## 🔧 Шаги развертывания

### 1. Подготовка репозитория

Убедитесь, что все файлы закоммичены и запушены в GitHub:

```bash
git add .
git commit -m "Prepare for Render deployment"
git push origin main
```

### 2. Создание сервиса на Render

1. Войдите в [Render Dashboard](https://dashboard.render.com)
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите ваш GitHub репозиторий
4. Выберите репозиторий с проектом

### 3. Настройка сервиса

**Основные настройки:**

- **Name:** `song-selector-app` (или любое другое имя)
- **Region:** Выберите ближайший регион
- **Branch:** `main` (или ваша основная ветка)
- **Root Directory:** Оставьте пустым (корень проекта)
- **Runtime:** `Python 3`
- **Build Command:** `pip install -r requirements.txt`
- **Start Command:** `cd Source && gunicorn -w 2 -b 0.0.0.0:$PORT --timeout 120 web_app:app`

**Переменные окружения:**

Добавьте в раздел **Environment Variables**:

- `GOOGLE_API_KEY` = `ваш_google_api_ключ`
- `FLASK_DEBUG` = `false`
- `PYTHON_VERSION` = `3.11.0` (опционально)

### 4. План и настройки

- **Instance Type:** Выберите подходящий план (Free tier доступен, но с ограничениями)
- **Auto-Deploy:** Включите, если хотите автоматическое развертывание при push

### 5. Развертывание

1. Нажмите **"Create Web Service"**
2. Render начнет процесс развертывания
3. Следите за логами в реальном времени
4. После успешного развертывания вы получите URL вида: `https://your-app-name.onrender.com`

## 🔍 Проверка развертывания

После развертывания проверьте:

1. **Health check:** `https://your-app-name.onrender.com/api/health`
   - Должен вернуть: `{"status": "ok", "message": "Система готова к работе"}`

2. **Главная страница:** `https://your-app-name.onrender.com`
   - Должен открыться интерфейс поиска песен

## 🐛 Устранение проблем

### Ошибка: "Индекс не найден"

**Проблема:** Файлы индекса не закоммичены в репозиторий.

**Решение:**
1. Убедитесь, что файлы `Data/songs_index.faiss` и `Data/songs_metadata.json` существуют локально
2. Временно уберите их из `.gitignore`
3. Закоммитьте и запушьте файлы
4. Переразверните приложение на Render

### Ошибка: "GOOGLE_API_KEY не установлен"

**Проблема:** Переменная окружения не настроена.

**Решение:**
1. В Render Dashboard перейдите в настройки вашего сервиса
2. Откройте раздел **Environment**
3. Добавьте переменную `GOOGLE_API_KEY` со значением вашего ключа
4. Сохраните и перезапустите сервис

### Ошибка: Timeout при запуске

**Проблема:** Приложение долго инициализируется.

**Решение:**
1. Увеличьте timeout в Procfile: `--timeout 180`
2. Проверьте размер файлов индекса (они не должны быть слишком большими)
3. Используйте более мощный план на Render

### Ошибка: "Module not found"

**Проблема:** Зависимости не установлены.

**Решение:**
1. Проверьте, что `requirements.txt` содержит все необходимые пакеты
2. Проверьте логи сборки в Render Dashboard
3. Убедитесь, что используется правильная версия Python

## 📝 Альтернативный способ: Build Script

Если вы хотите создавать индекс на сервере при развертывании, создайте файл `build.sh`:

```bash
#!/bin/bash
# build.sh
pip install -r requirements.txt
cd Source
python prepare_embeddings.py
```

И укажите его как Build Command в настройках Render.

**⚠️ Внимание:** Это займет больше времени при каждом развертывании и потребует наличия `Data/Songs.json` в репозитории.

## 🔄 Обновление приложения

После изменений в коде:

1. Закоммитьте изменения:
   ```bash
   git add .
   git commit -m "Update app"
   git push
   ```

2. Если включен Auto-Deploy, Render автоматически переразвернет приложение
3. Иначе нажмите **"Manual Deploy"** в Render Dashboard

## 💰 Стоимость

- **Free tier:** Доступен, но приложение "засыпает" после 15 минут бездействия
- **Starter ($7/мес):** Приложение всегда активно, больше ресурсов
- **Professional ($25/мес):** Еще больше ресурсов и возможностей

## 📚 Дополнительные ресурсы

- [Render Documentation](https://render.com/docs)
- [Gunicorn Configuration](https://docs.gunicorn.org/en/stable/settings.html)
- [Flask Deployment](https://flask.palletsprojects.com/en/latest/deploying/)

## ✅ Чеклист перед развертыванием

- [ ] Файлы индекса созданы локально
- [ ] Файлы индекса закоммичены в репозиторий (или убраны из .gitignore)
- [ ] Все зависимости указаны в `requirements.txt`
- [ ] `Procfile` создан и настроен
- [ ] `web_app.py` использует переменную `PORT`
- [ ] Google API ключ готов
- [ ] Код закоммичен и запушен в GitHub
- [ ] Репозиторий подключен к Render

---

**Удачи с развертыванием! 🎉**

