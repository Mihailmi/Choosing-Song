# 🚀 Развертывание на Render - Краткая инструкция

## Быстрый старт

### 1. Подготовка файлов индекса

Перед развертыванием нужно закоммитить файлы индекса в репозиторий:

```bash
# Убедитесь, что индекс создан локально
cd Source
python prepare_embeddings.py

# Временно уберите из .gitignore строки с файлами индекса:
# Data/songs_index.faiss
# Data/songs_metadata.json

# Добавьте файлы в репозиторий
git add Data/songs_index.faiss Data/songs_metadata.json
git commit -m "Add index files for deployment"
git push
```

### 2. Создание сервиса на Render

1. Зайдите на [render.com](https://render.com) и войдите в аккаунт
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите ваш GitHub репозиторий
4. Выберите репозиторий с проектом

### 3. Настройки сервиса

**Build Command:**
```
pip install -r requirements.txt
```

**Start Command:**
```
cd Source && gunicorn -w 2 -b 0.0.0.0:$PORT --timeout 120 web_app:app
```

**Environment Variables:**
- `GOOGLE_API_KEY` = ваш Google API ключ
- `FLASK_DEBUG` = `false`

### 4. Развертывание

Нажмите **"Create Web Service"** и дождитесь завершения развертывания.

После успешного развертывания вы получите URL вида:
`https://your-app-name.onrender.com`

## ⚠️ Важные моменты

1. **Файлы индекса** должны быть закоммичены в репозиторий
2. **Google API ключ** должен быть установлен в переменных окружения
3. **Free tier** на Render "засыпает" после 15 минут бездействия (первый запрос может быть медленным)

## 🔍 Проверка

После развертывания проверьте:
- `https://your-app-name.onrender.com/api/health` - должен вернуть `{"status": "ok"}`
- `https://your-app-name.onrender.com` - должен открыться интерфейс

## 📚 Подробная инструкция

См. файл `DEPLOY.md` для детальной информации и решения проблем.

