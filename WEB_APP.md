# 🌐 Веб-приложение для поиска песен

## Быстрый старт

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Убедитесь, что индекс создан

Если вы ещё не создали индекс, выполните:

```bash
cd Source
python prepare_embeddings.py
```

### 3. Запустите веб-сервер

```bash
cd Source
python web_app.py
```

### 4. Откройте браузер

Перейдите по адресу: **http://localhost:5000**

## Возможности

- 🎨 **Современный интерфейс** - тёмная тема с градиентами
- 🔍 **Быстрый поиск** - семантический поиск по песням
- 📋 **Просмотр кандидатов** - все найденные песни отображаются в виде карточек
- ⭐ **Выбранная песня** - выделяется и показывается с полным текстом
- 💭 **Объяснение** - LLM объясняет, почему выбрана именно эта песня
- 📱 **Адаптивный дизайн** - работает на десктопе и мобильных устройствах

## API Endpoints

### POST `/api/search`
Поиск песен по запросу.

**Запрос:**
```json
{
  "query": "Хочу что-то спокойное для вечера"
}
```

**Ответ:**
```json
{
  "candidates": [...],
  "selected": {...},
  "reasoning": "...",
  "message": "Поиск выполнен успешно"
}
```

### GET `/api/health`
Проверка состояния системы.

**Ответ:**
```json
{
  "status": "ok",
  "message": "Система готова к работе"
}
```

## Настройка порта

По умолчанию сервер запускается на порту 5000. Чтобы изменить порт, отредактируйте последнюю строку в `Source/web_app.py`:

```python
app.run(debug=True, host='0.0.0.0', port=5000)  # Измените порт здесь
```

## Развёртывание на сервере

Для production использования рекомендуется:

1. Отключить debug режим:
   ```python
   app.run(debug=False, host='0.0.0.0', port=5000)
   ```

2. Использовать WSGI сервер (например, Gunicorn):
   ```bash
   pip install gunicorn
   gunicorn -w 4 -b 0.0.0.0:5000 web_app:app
   ```

3. Настроить reverse proxy (Nginx) для статических файлов

4. Использовать переменные окружения для конфигурации

## Устранение неполадок

### Ошибка: "Индекс не найден"
Убедитесь, что вы запустили `prepare_embeddings.py` и файлы `songs_index.faiss` и `songs_metadata.json` существуют в папке `Data/`.

### Ошибка: "GOOGLE_API_KEY не установлен"
Создайте файл `.env` в корне проекта и добавьте:
```
GOOGLE_API_KEY=your_key_here
```

### Порт уже занят
Измените порт в `web_app.py` или остановите процесс, использующий порт 5000.

