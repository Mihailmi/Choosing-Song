# 🎵 RAG Система Выбора Песен

Современная система выбора песен на основе **RAG (Retrieval-Augmented Generation)** с использованием embeddings и LLM reasoning.

## 🎯 Что это?

Это не обучение модели с нуля, а правильный инженерный подход:
- **Semantic Search** через embeddings для поиска релевантных песен
- **LLM Reasoning** для выбора лучшей песни из кандидатов
- **Масштабируемо** и **дешево** в эксплуатации

## 🏗️ Архитектура

```
JSON (200 песен)
   ↓
Embeddings (один раз)
   ↓
Vector DB (FAISS)
   ↓
User request → Embedding запроса
   ↓
Top-K похожих песен
   ↓
LLM выбирает 1 + объясняет
```

## 📦 Установка

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Настройте API ключ

Создайте файл `.env` в корне проекта:

```env
GOOGLE_API_KEY=your_google_api_key_here
```

### 3. Подготовьте данные

Убедитесь, что файл `Data/Songs.json` содержит песни. Минимально необходимые поля:

```json
[
  {
    "id": 1,
    "title": "Название песни",
    "lyrics": "Текст песни..."  // или массив строк
  }
]
```

**Важно:** Поля `themes`, `mood`, `artist`, `language` **не обязательны** - система работает на основе текста песен (`lyrics`) и названий (`title`). 

Эти поля можно добавить позже для улучшения результатов (см. `ENRICHMENT.md`).

## 🚀 Использование

### Шаг 1: Создание embeddings (один раз)

Запустите скрипт для создания векторной БД:

```bash
cd Source
python prepare_embeddings.py
```

Это создаст:
- `Data/songs_index.faiss` - FAISS индекс
- `Data/songs_metadata.json` - метаданные песен

**Важно:** Это делается один раз! При добавлении новых песен запустите снова.

### Шаг 2: Поиск и выбор песен

#### Вариант A: Веб-интерфейс (рекомендуется)

Запустите веб-сервер:

```bash
cd Source
python web_app.py
```

Откройте браузер и перейдите по адресу: **http://localhost:5000**

Веб-интерфейс предоставляет:
- 🎨 Современный и красивый UI
- 🔍 Интерактивный поиск песен
- 📋 Отображение всех кандидатов
- ⭐ Выделение выбранной песни
- 💭 Объяснение выбора LLM

#### Вариант B: Консольный интерфейс

Запустите основной скрипт:

```bash
cd Source
python main.py
```

Введите ваш запрос, например:
- "Хочу что-то спокойное для вечера"
- "Нужна песня про любовь и расставание"
- "Ищу что-то энергичное для тренировки"

Система:
1. Найдёт 5 наиболее релевантных песен
2. Использует LLM для выбора лучшей
3. Объяснит, почему именно эта песня подходит

## 📁 Структура проекта

```
.
├── Data/
│   ├── Songs.json              # Исходные данные с песнями
│   ├── songs_index.faiss       # FAISS индекс (создаётся)
│   └── songs_metadata.json     # Метаданные (создаётся)
├── Source/
│   ├── __init__.py
│   ├── embeddings_manager.py   # Управление embeddings
│   ├── song_search.py          # Поиск песен
│   ├── song_selector.py        # Выбор лучшей через LLM
│   ├── prepare_embeddings.py   # Скрипт подготовки
│   ├── main.py                 # Основной скрипт (консоль)
│   ├── web_app.py              # Веб-сервер (Flask)
│   ├── templates/              # HTML шаблоны
│   │   └── index.html
│   └── static/                 # CSS и JavaScript
│       ├── style.css
│       └── script.js
├── requirements.txt
├── .env                        # API ключи (создайте сами)
└── README.md
```

## 🧠 Используемые модели

### Embeddings
- **Google embedding-001** (768 размерность)
- Можно заменить на bge-large, E5-large (open-source)

### LLM
- **Gemini 2.5 Flash** (по умолчанию)
- Автоматическое переключение на резервные модели при ошибках
- Поддерживает: gemini-2.5-flash, gemini-2.0-flash, gemini-2.5-pro

## 🔧 Настройка

### Изменение модели LLM

В `Source/main.py` или `Source/song_selector.py`:

```python
selector = SongSelector(api_key=api_key, model="gpt-4o")
```

### Изменение количества кандидатов

В `Source/main.py`:

```python
candidates = search_engine.search(query, k=10)  # Вместо 5
```

### Добавление фильтров

В `Source/main.py` используйте `search_with_filters`:

```python
candidates = search_engine.search_with_filters(
    query, 
    k=5,
    language="ru",
    mood=["спокойная", "задумчивая"]
)
```

## 💡 Почему это работает лучше обучения?

| Подход | Проблемы |
|--------|----------|
| Fine-tuning | Дорого, не гибко, нужно переобучать при изменениях |
| Классификаторы | Не понимают нюансы, требуют размеченных данных |
| Правила | Не масштабируются, хрупкие |
| **RAG** | ✅ Гибко, умно, масштабируемо, объяснимо |

## 🎓 Что вы изучаете

- **Embeddings** и семантический поиск
- **Vector databases** (FAISS)
- **RAG архитектуру**
- **Prompt engineering**
- **LLM reasoning**

Это современная AI-инженерия, а не Data Science!

## 🚀 Дальнейшее развитие

- [x] Веб-интерфейс
- [x] API endpoint
- [ ] Фильтры (язык, настроение, исполнитель) в веб-интерфейсе
- [ ] Диалоговый режим с контекстом
- [ ] Память пользователя (история запросов)
- [ ] Feedback loop (лайк/дизлайк)
- [ ] Локальные модели (LLaMA, bge)
- [ ] Развёртывание на сервере (Docker, cloud)

## 📝 Лицензия

Образовательный проект для изучения RAG и AI-инженерии.

## 🤝 Вклад

Это учебный проект. Экспериментируйте, улучшайте, делитесь результатами!

