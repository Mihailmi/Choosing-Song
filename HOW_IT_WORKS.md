# 🎵 Как работает это приложение

## 📚 Обзор архитектуры

Это **RAG (Retrieval-Augmented Generation)** система для выбора песен. Она использует комбинацию:
- **Semantic Search** (семантический поиск) через embeddings
- **LLM Reasoning** (рассуждения LLM) для финального выбора

### Почему RAG, а не обучение модели?

| Подход | Проблемы |
|--------|----------|
| Fine-tuning | Дорого, не гибко, нужно переобучать при изменениях |
| Классификаторы | Не понимают нюансы, требуют размеченных данных |
| Правила | Не масштабируются, хрупкие |
| **RAG** | ✅ Гибко, умно, масштабируемо, объяснимо |

---

## 🔄 Процесс работы (Pipeline)

### Этап 1: Подготовка данных (один раз)

```
JSON файл с песнями
    ↓
prepare_embeddings.py
    ↓
Для каждой песни:
  - Извлекается текст (название + lyrics + themes + mood)
  - Создаётся embedding через Google embedding-001 API
  - Вектор (768 чисел) сохраняется
    ↓
FAISS индекс (быстрый векторный поиск)
    ↓
songs_index.faiss + songs_metadata.json
```

**Что такое embedding?**
- Это числовое представление текста (вектор из 768 чисел)
- Похожие тексты имеют похожие векторы
- Можно измерять "расстояние" между текстами

**Пример:**
- "песня про любовь" → [0.12, -0.45, 0.78, ...] (768 чисел)
- "романтическая музыка" → [0.15, -0.42, 0.81, ...] (похожий вектор!)

### Этап 2: Поиск песен (при каждом запросе)

```
Пользователь: "Хочу что-то спокойное для вечера"
    ↓
1. Создаётся embedding запроса
   "Хочу что-то спокойное для вечера" → вектор
    ↓
2. Поиск в FAISS индексе
   - Сравнивается вектор запроса с векторами всех песен
   - Находятся Top-5 наиболее похожих (по косинусному расстоянию)
    ↓
3. Получение кандидатов
   - Извлекаются метаданные найденных песен
   - Возвращается список из 5 песен
```

**Код:** `song_search.py` → `SongSearch.search()`

### Этап 3: Выбор лучшей песни (LLM Reasoning)

```
5 кандидатов + запрос пользователя
    ↓
SongSelector.choose_best()
    ↓
Промпт для LLM (Gemini):
  "Вот 5 песен, найденных по запросу '...'
   Выбери ОДНУ лучшую и объясни почему"
    ↓
LLM анализирует и возвращает:
  - Номер выбранной песни
  - Объяснение выбора
    ↓
Результат пользователю
```

**Код:** `song_selector.py` → `SongSelector.choose_best()`

---

## 🏗️ Компоненты системы

### 1. `EmbeddingsManager` (`embeddings_manager.py`)
- Создаёт embeddings через Google API
- Управляет FAISS индексом
- Загружает/сохраняет данные

**Ключевые методы:**
- `create_embeddings()` - создаёт векторы для всех песен
- `build_index()` - строит FAISS индекс
- `get_query_embedding()` - создаёт вектор для запроса

### 2. `SongSearch` (`song_search.py`)
- Выполняет семантический поиск
- Использует FAISS для быстрого поиска похожих векторов

**Ключевые методы:**
- `search()` - базовый поиск
- `search_with_filters()` - поиск с фильтрами (язык, настроение)

### 3. `SongSelector` (`song_selector.py`)
- Использует LLM для выбора лучшей песни
- Автоматическое переключение между моделями при ошибках
- Парсинг ответа LLM

**Особенности:**
- Fallback на резервные модели при перегрузке
- Retry логика с экспоненциальной задержкой
- Поддержка множества моделей Gemini

### 4. Веб-интерфейс (`web_app.py`)
- Flask сервер
- REST API endpoint `/api/search`
- Интеграция с фронтендом

---

## 🔑 Используемые API

### Google Generative AI
1. **embedding-001** - для создания embeddings
   - Размерность: 768
   - Стоимость: ~$0.0001 за 1000 токенов

2. **Gemini 2.5 Flash** - для выбора песни
   - Быстрая и дешёвая модель
   - Автоматический fallback на другие модели

---

## 💡 Ключевые концепции AI

### 1. Embeddings (Векторные представления)
```python
# Текст → Вектор
"песня про любовь" → [0.12, -0.45, 0.78, ..., 0.33]  # 768 чисел

# Похожие тексты = близкие векторы
расстояние("любовь", "романтика") < расстояние("любовь", "война")
```

### 2. Vector Search (Векторный поиск)
- FAISS использует L2 расстояние (евклидово)
- Находит ближайшие векторы за O(log n) время
- Масштабируется до миллионов векторов

### 3. RAG Pattern
```
Retrieval (Поиск) → Augmentation (Обогащение) → Generation (Генерация)
     ↓                      ↓                          ↓
Найти похожие          Добавить контекст          LLM выбирает
   песни                  в промпт                  лучшую
```

### 4. Prompt Engineering
Промпт структурирован так, чтобы LLM:
- Понял задачу
- Получил все необходимые данные
- Вернул ответ в нужном формате

---

## 📊 Поток данных

```
┌─────────────┐
│  Songs.json │  (200+ песен)
└──────┬──────┘
       │
       ▼
┌─────────────────────┐
│ prepare_embeddings  │  (один раз)
│  - создаёт векторы  │
│  - строит индекс    │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ songs_index.faiss  │  (векторная БД)
│ songs_metadata.json │  (метаданные)
└──────┬──────────────┘
       │
       │ (при запросе)
       ▼
┌─────────────────────┐
│  User Query         │  "Хочу спокойную песню"
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  SongSearch         │  → Top-5 кандидатов
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  SongSelector       │  → Лучшая песня + объяснение
│  (LLM Reasoning)    │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Результат          │  Песня + объяснение
└─────────────────────┘
```

---

## 🎯 Преимущества этой архитектуры

1. **Масштабируемость** - легко добавить новые песни (пересоздать индекс)
2. **Гибкость** - не нужно переобучать модель при изменениях
3. **Объяснимость** - LLM объясняет, почему выбрана именно эта песня
4. **Эффективность** - FAISS очень быстрый поиск
5. **Дешевизна** - embeddings создаются один раз, LLM вызывается только для финального выбора

---

## 🚀 Что можно улучшить?

См. раздел "Предложения по улучшению" ниже.
